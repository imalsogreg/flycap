module Main where

import System.FlyCap
import CV.HighGUI

import System.Environment

main :: IO ()
main = do

--  [inFilename, outFilename] <- getArgs
--  opt <- createAVIContext 30.0
  
  --inMovieH <- openAVI "test.avi"
  let a = 'a' + 1
  return ()